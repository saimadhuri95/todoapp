# v0.1.0 Manual Test Matrix

Release: `v0.1.0`

Date:

Tester:

Notes:

## Alarms

- [ ] Android fresh install: enable notifications/exact alarms, schedule a due todo, app closed, alarm fires.
- [ ] Android reboot: existing due todo still fires after reboot.
- [ ] Android disable toggle: scheduled alarms are canceled.
- [ ] iOS fresh install: enable notifications, schedule a due todo, app backgrounded/closed, alarm fires.
- [ ] iOS deny-then-grant flow works after visiting Settings.
- [ ] macOS opt-in notifications: due todo fires after enable.
- [ ] macOS disable toggle cancels future alarms.
- [ ] Windows opt-in notifications: in-app alarm fires while the app is running.
- [ ] Windows disable toggle cancels future alarms.

## Pairing and Sync

- [ ] Android -> Windows QR pairing succeeds.
- [ ] Windows -> Android QR or paste pairing succeeds.
- [ ] iPhone <-> Mac pairing succeeds both directions.
- [ ] macOS QR invitation renders in the installed build.
- [ ] Android mailbox sync via Google Drive or synced folder propagates changes.
- [ ] iPhone <-> Mac iCloud Drive sync propagates changes.
- [ ] Linux plain synced folder (Dropbox, Syncthing, etc.) propagates changes.
- [ ] Device revoke rotates the group key and the revoked device stops receiving new changes.

## Permissions

- [ ] Android notifications denied initially, then granted later.
- [ ] Android exact alarm permission revoked and re-granted flow behaves correctly.
- [ ] iOS notifications denied initially, then granted later.
- [ ] macOS notifications denied initially, then granted later.
- [ ] Camera permission denied, QR scan screen shows the fallback guidance.
- [ ] Local-network prompt appears on Apple platforms and LAN sync still works after allow.
- [ ] Folder picker permission survives relaunch on sandboxed macOS builds.

## Restart and Persistence

- [ ] Launch -> add todo -> complete -> relaunch preserves the completed item.
- [ ] Sync folder selection persists across relaunch.
- [ ] Sync auto-resumes after relaunch without re-pairing.
- [ ] Desktop alarm toggle state persists across relaunch.
